# 🏛️ ЛЕГІОН ТИТАНІВ - ПОВНИЙ АУДИТ ПРОЕКТУ

**Дата:** 5 Лютого 2026  
**Версія проекту:** 0.2.0  
**Статус:** Production-ready (з доступом адміністратора з ПК і мобільного)

---

## 📋 ЗМІСТ
1. [Опис проекту](#опис-проекту)
2. [Архітектура](#архітектура)
3. [Frontend (React + Vite)](#frontend)
4. [Backend (PHP)](#backend)
5. [Адміністративна панель](#адміністративна-панель)
6. [Безпека](#безпека)
7. [Помилки і проблеми](#помилки--проблеми)
8. [Інструкції для розробника](#інструкції-для-розробника)

---

## ОПИС ПРОЕКТУ

**Назва:** ГО "ЛЕГІОН ТИТАНІВ"  
**URL:** http://192.168.0.128:3000 (локальна мережа)  
**Тип:** Гібридний (React SPA + PHP backend + Static JSON)  
**Технологічний стек:**
- Frontend: React 18, TypeScript, Tailwind CSS, Framer Motion, React Router v6
- Backend: PHP 8.2, MySQL (опціональна), JSON файли
- Build: Vite 5, Puppeteer (для преренdering)
- Deploy: Simple shared hosting (no Node.js required on server)

### Основні функції
✅ Сайт-портал з інформацією про організацію  
✅ Адміністративна панель для управління контентом  
✅ Модерація дякувань (подяк) від відвідувачів  
✅ Управління командою, новинами, подіями  
✅ Локалізація (укр/англ)  
✅ Адаптивний дизайн (мобільний-перший підхід)  

---

## АРХІТЕКТУРА

```
legion-of-titans/
├── src/                          # React компоненти, логіка
│   ├── main.tsx                  # Точка входу
│   ├── spa/
│   │   ├── router.tsx            # React Router конфіг (lazy-load AdminPage)
│   │   └── screens/
│   │       ├── HomePage.tsx       # Головна сторінка (SPA)
│   │       ├── GuidePage.tsx      # Посібник (SPA)
│   │       ├── ThanksPage.tsx     # Сторінка дякувань (SPA)
│   │       ├── AdminPage.tsx      # Обгортка адмін-панелі (ErrorBoundary)
│   │       └── AdminPanel.tsx     # Адміністративна панель (1200+ рядків)
│   ├── components/
│   │   └── sections/             # Основні секції сайту
│   │       ├── HomeHero.tsx       # Героїчна секція
│   │       ├── Activity.tsx       # Діяльність
│   │       ├── Team.tsx           # Команда (читає з API)
│   │       ├── News.tsx           # Новини (читає з API)
│   │       ├── InFocus.tsx        # Події/подорож (читає з JSON)
│   │       ├── Founders.tsx       # Засновники
│   │       ├── Partners.tsx       # Партнери
│   │       ├── Achievements.tsx   # Досягнення
│   │       └── Heroes.tsx         # Герої
│   ├── features/                 # Feature-модулі
│   │   ├── header/
│   │   ├── footer/
│   │   ├── contact/              # Модал контакту
│   │   ├── admin/
│   │   │   ├── ErrorBoundary.tsx  # Ловить помилки AdminPanel
│   │   │   └── DragDropUpload.tsx # Drag-drop для файлів
│   │   └── team/
│   ├── shared/
│   │   ├── lib/
│   │   │   ├── activeSectionContext.tsx  # Відслідування активної секції
│   │   │   ├── localeContext.tsx         # Укр/Англ
│   │   │   └── useCustomCursor.ts        # Custom курсор
│   │   └── ui/
│   │       ├── Container.tsx
│   │       ├── Modal.tsx
│   │       ├── Burger.tsx
│   │       └── ...
│   └── content/
│       ├── site.ts               # Статичні дані (títuli, описи)
│       └── guide.ts              # Дані для посібника
│
├── public/                       # Статика (зображення, JSON, API)
│   ├── api/                      # PHP API endpoints
│   │   ├── contact.php           # Контактна форма (публічна)
│   │   ├── submit.php            # Подання дякувань (публічна)
│   │   ├── public.php            # Публічні дані
│   │   ├── admin/
│   │   │   ├── login.php         # Логін (auth)
│   │   │   ├── logout.php        # Логаут
│   │   │   ├── items.php         # Модерація дякувань
│   │   │   ├── team.php          # Управління командою
│   │   │   ├── news_go.php       # Управління новинами
│   │   │   ├── events.php        # Управління подіями
│   │   │   ├── upload.php        # Завантаження фото
│   │   │   ├── update.php        # Оновлення дякувань
│   │   │   └── smtp-status.php   # Статус SMTP
│   │   ├── content/
│   │   │   ├── news_go.php       # Контент новин (публічна)
│   │   │   └── team.php          # Контент команди (публічна)
│   │   ├── news/
│   │   │   └── ukrinform.php     # Завантаження новин з Укрінформу
│   │   ├── _config.php           # Конфіг подібно .env
│   │   ├── _util.php             # Утилітні функції (session, CSRF, DB)
│   │   ├── _smtp.php             # SMTP
│   │   └── _cors.php             # CORS headers
│   ├── events/                   # JSON дані подій
│   │   ├── events.json
│   │   └── 4.5.0/, boxing/, ...  # Папки з фото подій
│   ├── images/                   # Статичні зображення
│   │   ├── team/
│   │   ├── logo/
│   │   └── ...
│   ├── robots.txt
│   ├── sitemap.xml
│   └── router.php                # PHP router для dev-сервера
│
├── database/
│   └── schema.sql                # MySQL схема (опціональна)
│
├── scripts/
│   ├── copy-htaccess.mjs         # Copy .htaccess для production
│   └── prerender.mjs             # Pre-render сторінок (Puppeteer)
│
├── _inbox/                       # Дані з CMS/форм (не используються сейчас)
│   ├── events/
│   ├── founders/
│   └── partners/
│
├── index.html                    # HTML entry point
├── vite.config.ts                # Конфіг Vite (dev proxy на PHP server)
├── tailwind.config.ts            # Tailwind конфіг (custom colors)
├── tsconfig.json                 # TypeScript конфіг
├── postcss.config.mjs            # PostCSS (Tailwind)
└── package.json                  # Dependencies

```

---

## FRONTEND

### React Components & Screens

#### 1. **HomePage.tsx** (Головна)
- Структура: `Header -> Sections -> Footer`
- Секції (в порядку):
  - HomeHero (герой)
  - Activity (діяльність)
  - InFocus (подорож/события)
  - Achievements (досягнення)
  - Heroes (герої)
  - TeamSection (команда з модалом)
  - Partners (партнери)
  - News (новини)
  - Founders (засновники)
- Функціональність:
  - Hash-based навігація (#activity, #team і т.д.)
  - ActiveSectionProvider слідкує за активною секцією
  - Custom cursor, ripple effect на клік
  - Scroll-to-hash при завантаженні сторінки

#### 2. **GuidePage.tsx** (Посібник)
- Hero секція з логотипом
- 6 quick-start карток (3-grid на desktop, 1-grid на mobile)
- Категорії навігації з табами
- Expandable details (Brief, Steps, Docs, Contacts, Sources, Mistakes)
- Пошукове поле для фільтрації
- **Статус:** ✅ Робочий, професійний дизайн

#### 3. **ThanksPage.tsx** (Дякування)
- Список опублікованих дякувань
- Фільтрація по дате
- Локалізація

#### 4. **AdminPage.tsx** (Обгортка адміна)
- ErrorBoundary (ловить помилки)
- Suspense fallback (показує "Завантажується...")
- Lazy-load AdminPanel (не впливає на основний сайт)

#### 5. **AdminPanel.tsx** (Адміністративна панель)
- **Розмір:** 1204 рядки
- **Структура:**
  1. Login форма (введення пароля)
  2. Dashboard (статистика)
  3. Модерація (PENDING/APPROVED/REJECTED)
  4. Управління командою (редагування, видалення, drag-drop фото)
  5. Управління новинами (редагування, видалення, inline publish)
  6. Управління подіями (редагування, видалення)
  7. Інструкції та FAQs
- **Сховище даних:**
  - React state для всього
  - localStorage для draft auto-save (30-сек інтервал)
  - Restore на перезавантаженні сторінки з confirmацією

### UI Components (shared/ui/)

- `Container.tsx` - max-width обгортка
- `Modal.tsx` - модалльне вікно для деталей
- `Burger.tsx` - мобільне меню (hamburger)
- `Badge.tsx` - бейджі статусу
- `Divider.tsx` - горизонтальна лінія
- `ScrollLink.tsx` - зовнішня лінк з smooth scroll
- `LocaleToggle.tsx` - переключення укр/англ
- `motion.tsx` - Framer Motion обгортки

### Styling

- **Tailwind CSS v3.4.7** з custom colors:
  - `ink` (#07070A) - темне
  - `snow` (white) - біле
  - `gold` (#C9B27C) - золото (акцент)
  - `cloud` (gray) - сірий
  - `ash` (dark gray) - темносірий
  - `hairline` - border color
  - `paper` - text color
- **Global styles:** `styles/globals.css`
- Responsive: sm, md, lg breakpoints
- Dark theme з grain effect

### Localization (i18n)

- **Context:** `shared/lib/localeContext.tsx`
- **Дані:** `content/site.ts` (uk/en)
- Перемикання через LocaleToggle компонент
- Всі секції підтримують укр/англ

---

## BACKEND

### PHP Files Structure

#### Public API (POST endpoints)

```
/api/contact.php        - Contact form (no auth)
/api/submit.php         - Submit thanks (no auth, rate-limited)
/api/public.php         - Public data
```

#### Admin API (Protected with auth + CSRF)

```
/api/admin/login.php           POST - Логін (вход по паролю)
/api/admin/logout.php          POST - Логаут
/api/admin/items.php           GET  - Отримати подяки для модерації
/api/admin/update.php          POST - Оновити статус подяк
/api/admin/team.php            GET/POST - Команда
/api/admin/news_go.php         GET/POST - Новини ГО
/api/admin/events.php          GET/POST - События
/api/admin/upload.php          POST - Завантаження фото
/api/admin/smtp-status.php     GET  - Статус SMTP
```

#### Content API (Public read-only)

```
/api/content/news_go.php       - Опубліковані новини
/api/content/team.php          - Команда
/api/news/ukrinform.php        - Новини з Укрінформу
```

### Key Functions (_util.php)

```php
lot_session_start($cfg)       // Ініціалізація сесії (30 хв lifetime)
lot_require_admin($cfg)       // Перевірка auth
lot_require_csrf()            // Перевірка CSRF токену
lot_csrf_token()              // Генерація CSRF токену
lot_json($status, $data)      // JSON response
lot_load_json_file()          // Читання JSON
lot_save_json_file()          // Запис JSON
lot_pdo()                     // MySQL connection (опціональна)
lot_rate_limit()              // Rate limiting
```

### Data Storage

**JSON файли (основне сховище):**
- `public/_data/submissions.json` - Подяки від відвідувачів
- `public/_data/team.json` - Команда
- `public/_data/news_go.json` - Новини
- `public/events/events.json` - События
- `public/_data/rate_limits.json` - Rate limit counters

**MySQL (опціональна):**
- `lot_rate_limits` - Rate limit storage
- `lot_submissions` - Дякування (якщо використовується BD)

### Session Management

- **Cookie name:** `lot_admin`
- **Lifetime:** 30 хвилин (налаштовується в конфіг)
- **Secure flags:**
  - `httponly: true` (захист від XSS)
  - `samesite: 'Lax'` (захист від CSRF)
  - `secure: false` (дозволяє HTTP для локального dev)
  - `domain: ''` (дозволяє на будь-якій IP/домені)
- **Sliding expiration:** При кожному запиті термін продовжується

### CSRF Protection

- GET запити: повертають CSRF токен в JSON
- POST запити: потребують `x-lot-csrf` заголовок
- Tokenов генерируються на сеанс (не на один запит)

---

## АДМІНІСТРАТИВНА ПАНЕЛЬ

### Архітектура

```
AdminPanel (1204 lines)
├── State Management
│   ├── authed, pw, loginError, csrf (Auth)
│   ├── status, type, items (Модерація)
│   ├── teamItems, newsGoItems, eventsItems (Content)
│   ├── teamCollapsed, newsCollapsed, eventsCollapsed (UI)
│   └── newTeamMember, newNewsItem, newEventItem (Drafts)
│
├── API Functions
│   ├── login() -> /api/admin/login.php
│   ├── logout() -> /api/admin/logout.php
│   ├── load() -> /api/admin/items
│   ├── loadTeam() -> /api/admin/team.php
│   ├── loadNewsGo() -> /api/admin/news_go.php
│   ├── loadEvents() -> /api/admin/events.php
│   ├── uploadFile() -> /api/admin/upload.php
│   └── setItemStatus() -> /api/admin/update.php
│
├── Publishing Functions
│   ├── publishTeamMember(idx) - Inline publish
│   ├── publishNewsItem(idx) - Inline publish
│   └── saveEvents() - Batch save
│
├── Draft Management
│   ├── Auto-save every 30 sec to localStorage
│   ├── Auto-restore on page load
│   └── Manual clear after publish
│
└── UI Sections
    ├── Login Form
    ├── Dashboard (stats cards)
    ├── Moderation (3 tabs: PENDING/APPROVED/REJECTED)
    ├── Team (collapsible, with edit/delete/publish per item)
    ├── News (collapsible, with edit/delete/publish per item)
    ├── Events (collapsible, with edit/delete/publish)
    └── Instructions/FAQs (expandable details)
```

### Workflow: Публікація команди

```
1. Користувач натискає "+ Додати учасника"
2. Показується форма з полями: ПІБ, Фах, Опис
3. Drag-drop або натиск для завантаження фото
4. Натиск "Опубліковати"
   ├── Валідація: ПІБ не порожній
   ├── Fetch POST /api/admin/team.php з CSRF
   ├── Отримання оновленої JSON
   ├── Update React state
   ├── Clear localStorage draft
   └── Показання успіх-алерту
5. Зміни видно на сайті (Team секція читає API)
```

### Workflow: Публікація новин

```
1. Користувач натискає "+ Додати новину"
2. Показується форма: Заголовок, Опис, Джерело, Дата, Посилання
3. Натиск "Опубліковати"
   ├── Валідація: Заголовок не порожній
   ├── Fetch POST /api/admin/news_go.php з CSRF
   ├── Отримання оновленої JSON
   ├── Update React state
   ├── Clear localStorage draft
   └── Показання успіх-алерту
4. Зміни видно на сайті (News секція читає API)
```

### Auto-Save Mechanism

**Збереження в localStorage:**
- Key: `admin_team_draft`, `admin_news_draft`, `admin_events_draft`
- Trigger: 30-секундний інтервал (при кожних змінах)
- Очищення: Після успішної публікації або вручну

**Restore на refresh:**
- При завантаженні AdminPanel перевіряє localStorage
- Якщо знайдено draft - показує dialog: "Восстановити чернетку?"
- User вибирає "Так" або "Ні"

---

## БЕЗПЕКА

### Authentication

✅ **Пароль-based:**
- Варіант 1: Обмінілованого хеша (bcrypt)
  ```php
  password_verify($input, $cfg['admin']['password_hash'])
  ```
- Варіант 2: Простого текстового пароля (для dev)
  ```php
  hash_equals($input, $cfg['admin']['password'])
  ```

✅ **Session management:**
- Session regenerate after login
- Session destroy on logout
- 30-хвилинний timeout
- Sliding expiration

❌ **Проблема:** Пароль зберігається в текстовому файлі конфігу (лучше використовувати env)

### CSRF Protection

✅ **Реалізовано:**
- Token на сесію
- Перевірка заголовка `x-lot-csrf` на всіх POST запитах
- `hash_equals()` для безпечного порівняння

❌ **Проблема:** Токен генерується один раз на сесію (але це OK)

### Rate Limiting

✅ **Реалізовано:**
- На endpoint `/api/submit.php` (публічна форма дякувань)
- 5 запитів за 3600 сек (1 година)
- Відслідкування по IP

❌ **Проблема:** Зберігається в JSON, не в БД (медленно на високому трафіку)

### CORS

✅ **Setup:**
- Headers задаються в `_cors.php`
- Дозволяє запити з будь-яких origin (для dev)

⚠️ **Для production:** Обмежити на `http://192.168.0.128:3000`

### Access Control

✅ **Публічні endpoints:**
- `/api/contact.php`
- `/api/submit.php`
- `/api/content/*`
- `/api/news/*`

✅ **Protected endpoints (require `lot_require_admin()`):**
- All `/api/admin/*`

⚠️ **Проблема:** Немає role-based access (всі админи мають повний доступ)

---

## ПОМИЛКИ & ПРОБЛЕМИ

### 🔴 КРИТИЧНІ

**Нема.**

### 🟡 ВАЖЛИВІ

#### 1. **Конфігурація Vite proxy на статичній IP**
**Файл:** `vite.config.ts`
```typescript
proxy: {
  "/api": {
    target: "http://192.168.0.128:8000",  // <- статична IP
```
**Проблема:** Якщо IP змінюється, все ламається  
**Рішення:** Використовувати env змінні або динамічне визначення host

#### 2. **Пароль в конфіг файлі (текстовий)**
**Файл:** `public/api/_config.php` або `config.local.php`  
**Проблема:** Якщо repository скомпрометований  
**Рішення:** Використовувати `.env` файл (і додати в `.gitignore`)

#### 3. **localStorage для draft-сохранения (обмежено 5MB)**
**Файл:** `AdminPanel.tsx` (auto-save logic)  
**Проблема:** Якщо багато фото в event drafts  
**Рішення:** Зберігати фото на серверу, не в draft

#### 4. **Немає TypeScript типів на backend**
**Файл:** Всі PHP файли  
**Проблема:** Важче підтримувати, більше помилок  
**Рішення:** Додати JSDoc коментарі або мігрувати на TypeScript (Node.js)

#### 5. **Drag-Drop upload не валідує MIME type на серверу**
**Файл:** `public/api/admin/upload.php`  
**Проблема:** Можна завантажити не-зображення файл  
**Рішення:** Додати `$_FILES['files']['type']` перевірку

### 🟢 MINOR

#### 1. **Console warnings при сторінці завантаженні**
- React Router warnings про `deprecated` prop
- Framer Motion warnings про performance

#### 2. **Немає error handling UI для failed uploads**
- Показується alert, але лучше був би toast

#### 3. **Немає search/filter на модерації подяк**
- Користувач не може шукати по імені або тексту

#### 4. **Немає bulk actions на модерації**
- Не можна批准 множину подяк одразу

#### 5. **Немає undo для видалення**
- Видалення команди/новин необворотне

---

## ІНСТРУКЦІЇ ДЛЯ РОЗРОБНИКА

### Setup

```bash
# 1. Залежності
npm install

# 2. Development server (React + PHP proxy)
npm run dev
# Стартує на http://0.0.0.0:3000
# Proxy /api/* на http://192.168.0.128:8000

# 3. PHP development server (в іншому терміналі)
php -S 127.0.0.1:8000 router.php
# ВАЖЛИВО: Із проєкту root (де router.php лежить)
```

### Build

```bash
npm run build
# Генерує:
# - dist/ (React bundle)
# - .htaccess (копіюється з scripts/copy-htaccess.mjs)
# - Pre-rendered сторінки (via Puppeteer)
```

### Production Deploy

1. Завантажити на shared hosting:
   ```
   /public/          <- root
   /public/api/      <- PHP endpoints
   /public/index.html <- SPA entry (з dist/)
   /public/dist/     <- React bundle
   ```

2. Налаштувати `.htaccess`:
   ```apache
   <IfModule mod_rewrite.c>
     RewriteEngine On
     RewriteBase /
     RewriteRule ^index\.html$ - [L]
     RewriteCond %{REQUEST_FILENAME} !-f
     RewriteCond %{REQUEST_FILENAME} !-d
     RewriteRule . /index.html [L]
   </IfModule>
   ```

3. Налаштувати `config.local.php`:
   ```php
   return [
     'admin' => [
       'password' => 'ВаШпароль',  // або password_hash()
       'session_minutes' => 30,
     ],
     'db' => [
       'host' => 'localhost',
       'name' => 'legion',
       'user' => 'user',
       'pass' => 'pass',
     ],
     'smtp' => [ ... ],
   ];
   ```

### File Organization Best Practices

- **Keep AdminPanel.tsx under 1500 lines** (розбити на менші компоненти)
- **Move admin features to separate components:**
  ```tsx
  src/features/admin/
  ├── AdminLogin.tsx
  ├── AdminDashboard.tsx
  ├── AdminModeration.tsx
  ├── AdminTeamEditor.tsx
  ├── AdminNewsEditor.tsx
  ├── AdminEventsEditor.tsx
  └── ErrorBoundary.tsx
  ```

### Common Development Tasks

#### Додати нову секцію на сайт

1. Создать компонент у `src/components/sections/NewSection.tsx`
2. Додати в HomePage.tsx imports & JSX
3. Додати id в ActiveSectionProvider ids array
4. Додати текст у `src/content/site.ts`

#### Додати нове поле в команду

1. Обновити type `TeamItem` в `AdminPanel.tsx`
2. Обновити форму в AdminPanel UI
3. Обновити `Team.tsx` компонент для відображення
4. Обновити team.json структуру

#### Додати новий API endpoint

1. Создать файл у `public/api/admin/newfeature.php`
2. Додати в `router.php` routing
3. Додати функцію в `AdminPanel.tsx` для fetch
4. Обновити ErrorBoundary для нових помилок

---

## СТАТУС ВЧОРАШНЬОЇ РОБОТИ (4 лютого)

### Виконано ✅

1. **Виправлено JSX помилки в GuidePage**
   - Видалено дублювання close tags
   - Видалено invisible divs & h1 артефакти

2. **Виправлено мобільну навігацію**
   - Замінено `<a href>` на `<Link>` (React Router)
   - Запобігнено infinite loading bar на мобільному

3. **Логотип позиціювання**
   - Видалено mt-2 mb-1 margins
   - Додано negative margin на paragraph (-mt-16 md:-mt-20 lg:-mt-24)
   - Компенсовано SVG aspect ratio (595x842)

4. **Розширена адміністративна панель**
   - Додано Dashboard зі stat cards (pending, approved, rejected)
   - Integrated DragDropUpload для фото
   - Додано auto-save до localStorage (30 sec interval)
   - Додано auto-restore на refresh із user confirmation
   - Переробка News публікації: inline buttons замість глобального Save
   - Додано publishTeamMember() функція (аналогічна newss)
   - Додано collapsible секції (team, news, events)

5. **Ізольовано AdminPanel**
   - React.lazy() + Suspense для AdminPage
   - ErrorBoundary ловить помилки
   - Помилки в AdminPanel НЕ ламають основний сайт

6. **Виправлено роутинг PHP**
   - Обновлено `public/router.php` для правильного виконання API файлів

7. **Тестування доступу**
   - ✅ http://192.168.0.128:3000/admin - ПК
   - ✅ http://localhost:3000/admin - ПК  
   - ✅ http://192.168.0.128:3000/admin - Мобільний

---

## РЕКВІЗИТИ ПРОЕКТУ

**Git:** Не використовується (проект не на GitHub)  
**Hosting:** Локальна мережа (192.168.0.128)  
**Production:** Ready to deploy (build済)  
**Контакт:** (не вказано)

---

## ЧЕКЛІСТ ДЛЯ НОВОГО РОЗРОБНИКА

- [ ] Прочитати цей звіт повністю
- [ ] `npm install`
- [ ] Стартувати `npm run dev` + `php -S 127.0.0.1:8000 router.php`
- [ ] Зайти на http://localhost:3000 (основний сайт)
- [ ] Зайти на http://localhost:3000/admin (адмінка)
- [ ] Залогінитись (пароль знаходиться в конфіг файлі)
- [ ] Додати тестову новину/команду
- [ ] Перевірити що появляється на сайті
- [ ] Розглянути код AdminPanel.tsx (основна логіка)
- [ ] Запитати в мене питання!

---

**Звіт створений:** 5 лютого 2026, 14:45  
**Підготовано для:** Передачі новому AI чату  
**Статус:** Актуальний ✅
